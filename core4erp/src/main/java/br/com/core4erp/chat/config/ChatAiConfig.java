package br.com.core4erp.chat.config;

import org.springframework.context.annotation.Configuration;

/**
 * Ponto de extensão para configurações futuras do Spring AI
 * (custom advisors, retry policies, model overrides).
 * A auto-configuração do starter resolve tudo via application.properties.
 */
@Configuration
public class ChatAiConfig {
}
